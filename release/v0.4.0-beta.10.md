# research-skills `v0.4.0-beta.10` Release Notes

Date: 2026-04-01
Stage: Beta

## Highlights (Draft)

- fix: improve cleanup function in bootstrap script and add test for partial profile from source repo (68d65c9)
- fix: add 'local' option to RefType in bootstrap script parameters and update tests (002fce7)
- feat: add support for local source repository in bootstrap scripts and update tests (187e2ed)
- fix: enhance dry-run output in bootstrap script and update tests to reflect changes (8a18127)
- docs: add notes on 'full' mode behavior for mise installation in README and installation guides (15370e1)
- feat: enhance bootstrap scripts with path management and user environment persistence (01af6d1)
- docs: update PowerShell instructions to specify version 7+ and enhance installation steps (d4497f1)
- feat: add PowerShell version check and enhance command execution handling in bootstrap script (5fa7573)

## Validation Evidence

Executed locally on 2026-04-01:

```bash
python3 scripts/validate_research_standard.py --strict
python3 -m unittest discover -s tests -v
./scripts/run_beta_smoke.sh
```

Observed result:

- `validate_research_standard.py --strict`: `${VALIDATOR_RESULT}`
- `unittest`: `${UNITTEST_RESULT}`
- `run_beta_smoke.sh`: `${SMOKE_RESULT}`

## Publish Steps

```bash
./scripts/release_ready.sh --version 0.4.0b10
git add pyproject.toml research_skills/__init__.py research-paper-workflow/VERSION skills/registry.yaml skills release/v0.4.0-beta.10.md
git commit -m "chore: prepare release 0.4.0b10"
git tag -a v0.4.0-beta.10 -m "research-skills release"
git push origin main --tags
./scripts/release_automation.sh post --tag v0.4.0-beta.10 --create-release
```

For rollback procedure, see `release/rollback.md`.

_Baseline tag for highlights: `v0.4.0-beta.9`_
