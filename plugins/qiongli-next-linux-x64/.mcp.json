{
  "mcpServers": {
    "qiongli-next": {
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/qiongli",
      "args": [
        "mcp",
        "serve",
        "--profile",
        "lite",
        "--transport",
        "stdio"
      ],
      "cwd": "${CLAUDE_PLUGIN_ROOT}"
    }
  }
}
