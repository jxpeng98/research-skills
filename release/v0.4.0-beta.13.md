# research-skills `v0.4.0-beta.13` Release Notes

Date: 2026-04-01
Stage: Beta

## Highlights (Draft)

- feat: add Refresh-SessionPath function and enhance Python installation checks (b6b46ae)

## Validation Evidence

Executed locally on 2026-04-01:

```bash
python3 scripts/validate_research_standard.py --strict
python3 -m unittest discover -s tests -v
./scripts/run_beta_smoke.sh
```

Observed result:

- `validate_research_standard.py --strict`: `${VALIDATOR_RESULT}`
- `unittest`: `${UNITTEST_RESULT}`
- `run_beta_smoke.sh`: `${SMOKE_RESULT}`

## Publish Steps

```bash
./scripts/release_ready.sh --version 0.4.0b13
git add pyproject.toml research_skills/__init__.py research-paper-workflow/VERSION skills/registry.yaml skills release/v0.4.0-beta.13.md
git commit -m "chore: prepare release 0.4.0b13"
git tag -a v0.4.0-beta.13 -m "research-skills release"
git push origin main --tags
./scripts/release_automation.sh post --tag v0.4.0-beta.13 --create-release
```

For rollback procedure, see `release/rollback.md`.

_Baseline tag for highlights: `v0.4.0-beta.12`_
