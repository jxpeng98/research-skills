# research-skills `v0.5.0-beta.3` Release Notes

Date: 2026-04-11
Stage: Beta

## Highlights (Draft)

- fix: Remove commented section for local agent artifacts in .gitignore (b965821)
- fix: Update .gitignore to include output/test_runtime/ directory (da60f75)
- feat: Add multi-agent smoke harness and related tests for local validation (6a68b6f)
- docs: Add Multi-Agent Runtime Guide and update documentation for Gemini transport and authentication (2b8bd53)
- feat: Add Gemini ACP backend support and enhance CLI options for session management (f858320)
- docs: Update troubleshooting documentation to include transport control options for Gemini execution paths (4645745)
- feat: Implement Gemini transport resolution and update profiles to use auto transport (a924501)
- feat: Enhance Gemini CLI integration and troubleshooting documentation (857c23f)

## Validation Evidence

Executed locally on 2026-04-11:

```bash
python3 scripts/validate_research_standard.py --strict
python3 -m unittest discover -s tests -v
./scripts/run_beta_smoke.sh
```

Observed result:

- `validate_research_standard.py --strict`: `${VALIDATOR_RESULT}`
- `unittest`: `${UNITTEST_RESULT}`
- `run_beta_smoke.sh`: `${SMOKE_RESULT}`

## Publish Steps

```bash
./scripts/release_ready.sh --version 0.5.0b3
git add pyproject.toml research_skills/__init__.py research-paper-workflow/VERSION skills/registry.yaml skills release/v0.5.0-beta.3.md
git commit -m "chore: prepare release 0.5.0b3"
git tag -a v0.5.0-beta.3 -m "research-skills release"
git push origin main --tags
./scripts/release_automation.sh post --tag v0.5.0-beta.3 --create-release
```

For rollback procedure, see `release/rollback.md`.

_Baseline tag for highlights: `v0.5.0-beta.2`_
