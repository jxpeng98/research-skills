# research-skills `v0.5.0-beta.2` Release Notes

Date: 2026-04-08
Stage: Beta

## Highlights (Draft)

- feat: Enhance installation scripts to support auto-upgrade of managed skills and enforce overwrite for unmanaged CLI (ca05322)

## Validation Evidence

Executed locally on 2026-04-08:

```bash
python3 scripts/validate_research_standard.py --strict
python3 -m unittest discover -s tests -v
./scripts/run_beta_smoke.sh
```

Observed result:

- `validate_research_standard.py --strict`: `${VALIDATOR_RESULT}`
- `unittest`: `${UNITTEST_RESULT}`
- `run_beta_smoke.sh`: `${SMOKE_RESULT}`

## Publish Steps

```bash
./scripts/release_ready.sh --version 0.5.0b2
git add pyproject.toml research_skills/__init__.py research-paper-workflow/VERSION skills/registry.yaml skills release/v0.5.0-beta.2.md
git commit -m "chore: prepare release 0.5.0b2"
git tag -a v0.5.0-beta.2 -m "research-skills release"
git push origin main --tags
./scripts/release_automation.sh post --tag v0.5.0-beta.2 --create-release
```

For rollback procedure, see `release/rollback.md`.

_Baseline tag for highlights: `v0.5.0-beta.1`_
